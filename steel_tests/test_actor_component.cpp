#include <catch2/catch.hpp>
#include "steel.hpp"

using namespace Steel;
TEST_CASE("test_actor_component", "") 
{
	/*ActorComponent component;
	REQUIRE(component.GetOwner() == nullptr);

	StrongActorPtr actor = CreateSharedPtr<Actor>();
	ActorComponent other_component(actor);
	REQUIRE(other_component.GetOwner() == actor);
	
	StrongActorPtr other_actor = CreateSharedPtr<Actor>();
	other_component.SetOwner(other_actor);
	
	REQUIRE(other_component.GetOwner() == other_actor);*/





}
